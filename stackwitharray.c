#include<stdio.h>
int main()
{   int size,i;
    int array[]={};
    printf("enter your size:");
    scanf("%d",&size);
for (int i=0;i<4;i++)
    {
    printf("print the size of the array",i+1);
    scanf("%d",array[i]);
    }



// for (i=0;i<size;i++)
// {
//     printf("%d",array[size]);
// }

// return 0;
}